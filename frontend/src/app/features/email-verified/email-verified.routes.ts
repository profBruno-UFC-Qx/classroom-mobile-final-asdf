import { Routes } from '@angular/router';
import { EmailVerifiedPageComponent } from './email-verified-page.component';

export const EMAIL_VERIFIED_ROUTES: Routes = [
  { path: '', component: EmailVerifiedPageComponent },
];
